<div class="table-responsive">
  <table class="table table-hover table-striped">
    <thead>
        <tr>
          <th scope="col">id</th>
          <th scope="col">mail</th>
          <th scope="col">Controle</th>
        </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
      <tr>
        <th scope="row"><%= user.id %></th>
        <td><%= user.email %></td>
        <td>
          <span class="ml-2"><%= link_to icon('far','edit'), edit_user_registration_path, class: "text-info" %></span>
          <span class="ml-2">
            <%= link_to cancel_user_registration_path, class: "text-danger", data: { confirm: "Voulez-vous supprimer #{user.email} ?" } do %>
              <%= icon('far','trash-alt') %>
            <% end %>
          </span>
        </td>
        <td>
          <span class="ml-2">
          </span>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
